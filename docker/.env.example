# 도커 스택 비밀값 템플릿
# 이 파일을 복사해서 docker/.env 를 만들고 실제 값을 채우세요:
#   cp .env.example .env
# (docker/.env 는 git 에 올리지 않습니다)

DB_PASSWORD=changeme
MYSQL_ROOT_PASSWORD=changeme
JWT_SECRET=changeme_random_secret
SESSION_SECRET=changeme_random_secret
