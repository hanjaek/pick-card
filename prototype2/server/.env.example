# 서버 포트 (프론트 vite.config.js의 proxy target과 일치해야 함)
PORT=4000

# MySQL 접속 정보
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=bnk_card

# JWT 서명 키 (실제 배포 시 충분히 긴 랜덤 문자열로 교체)
JWT_SECRET=change_this_to_a_long_random_secret
