{
  "name": "pickard",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"server,client\" -c \"blue,green\" \"npm run dev --prefix prototype/server\" \"npm run dev --prefix prototype/client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
